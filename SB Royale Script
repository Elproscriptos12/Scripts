-- Gui to Lua
-- Version: 3.2

-- Instances:

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local SRANTICHEAT = Instance.new("TextButton")
local UICorner_2 = Instance.new("UICorner")
local Esp = Instance.new("TextButton")
local UICorner_3 = Instance.new("UICorner")
local EspItems = Instance.new("TextButton")
local UICorner_4 = Instance.new("UICorner")
local Tittle = Instance.new("TextLabel")
local UICorner_5 = Instance.new("UICorner")
local ImageLabel = Instance.new("ImageLabel")
local X = Instance.new("TextButton")
local UICorner_6 = Instance.new("UICorner")
local GetALLItems = Instance.new("TextButton")
local UICorner_7 = Instance.new("UICorner")
local Open = Instance.new("TextButton")

--Properties:

ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = game.CoreGui

Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Frame.BorderColor3 = Color3.fromRGB(0, 0, 0)
Frame.BorderSizePixel = 0
Frame.Position = UDim2.new(0.225590914, 0, 0.211538464, 0)
Frame.Size = UDim2.new(0, 481, 0, 423)

UICorner.Parent = Frame

SRANTICHEAT.Name = "SR ANTICHEAT"
SRANTICHEAT.Parent = Frame
SRANTICHEAT.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
SRANTICHEAT.BorderColor3 = Color3.fromRGB(0, 0, 0)
SRANTICHEAT.BorderSizePixel = 0
SRANTICHEAT.Position = UDim2.new(0.291060299, 0, 0.0614657216, 0)
SRANTICHEAT.Size = UDim2.new(0, 200, 0, 50)
SRANTICHEAT.Font = Enum.Font.Unknown
SRANTICHEAT.Text = "Ultimate SR ANTICHEAT LEAKED "
SRANTICHEAT.TextColor3 = Color3.fromRGB(0, 0, 0)
SRANTICHEAT.TextScaled = true
SRANTICHEAT.TextSize = 14.000
SRANTICHEAT.TextWrapped = true

UICorner_2.Parent = SRANTICHEAT

Esp.Name = "Esp"
Esp.Parent = Frame
Esp.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Esp.BorderColor3 = Color3.fromRGB(0, 0, 0)
Esp.BorderSizePixel = 0
Esp.Position = UDim2.new(0.291060299, 0, 0.205673754, 0)
Esp.Size = UDim2.new(0, 200, 0, 50)
Esp.Font = Enum.Font.DenkOne
Esp.Text = "Esp All"
Esp.TextColor3 = Color3.fromRGB(0, 0, 0)
Esp.TextScaled = true
Esp.TextSize = 14.000
Esp.TextWrapped = true

UICorner_3.Parent = Esp

EspItems.Name = "Esp Items"
EspItems.Parent = Frame
EspItems.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
EspItems.BorderColor3 = Color3.fromRGB(0, 0, 0)
EspItems.BorderSizePixel = 0
EspItems.Position = UDim2.new(0.295218289, 0, 0.439716309, 0)
EspItems.Size = UDim2.new(0, 200, 0, 50)
EspItems.Font = Enum.Font.DenkOne
EspItems.Text = "Esp Items"
EspItems.TextColor3 = Color3.fromRGB(0, 0, 0)
EspItems.TextScaled = true
EspItems.TextSize = 14.000
EspItems.TextWrapped = true

UICorner_4.Parent = EspItems

Tittle.Name = "Tittle"
Tittle.Parent = Frame
Tittle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Tittle.BorderColor3 = Color3.fromRGB(0, 0, 0)
Tittle.BorderSizePixel = 0
Tittle.Position = UDim2.new(0.116716981, 0, -0.12032266, 0)
Tittle.Size = UDim2.new(0, 424, 0, 50)
Tittle.Font = Enum.Font.LuckiestGuy
Tittle.Text = "ElproScript Good For Exposing exploiters"
Tittle.TextColor3 = Color3.fromRGB(0, 0, 0)
Tittle.TextScaled = true
Tittle.TextSize = 14.000
Tittle.TextWrapped = true

UICorner_5.Parent = Tittle

ImageLabel.Parent = Tittle
ImageLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ImageLabel.BackgroundTransparency = 1.000
ImageLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
ImageLabel.BorderSizePixel = 0
ImageLabel.Position = UDim2.new(0.234999537, 0, -2.22000027, 0)
ImageLabel.Size = UDim2.new(0, 183, 0, 111)
ImageLabel.Image = "rbxassetid://14547802759"

X.Name = "X"
X.Parent = Tittle
X.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
X.BorderColor3 = Color3.fromRGB(0, 0, 0)
X.BorderSizePixel = 0
X.Position = UDim2.new(-0.139728338, 0, 0, 0)
X.Size = UDim2.new(0, 59, 0, 50)
X.Font = Enum.Font.SourceSans
X.Text = "X"
X.TextColor3 = Color3.fromRGB(0, 0, 0)
X.TextScaled = true
X.TextSize = 14.000
X.TextWrapped = true

UICorner_6.Parent = X

GetALLItems.Name = "Get ALL Items"
GetALLItems.Parent = Frame
GetALLItems.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
GetALLItems.BorderColor3 = Color3.fromRGB(0, 0, 0)
GetALLItems.BorderSizePixel = 0
GetALLItems.Position = UDim2.new(0.293139279, 0, 0.595744669, 0)
GetALLItems.Size = UDim2.new(0, 200, 0, 50)
GetALLItems.Font = Enum.Font.DenkOne
GetALLItems.Text = "Get More Power"
GetALLItems.TextColor3 = Color3.fromRGB(0, 0, 0)
GetALLItems.TextScaled = true
GetALLItems.TextSize = 14.000
GetALLItems.TextWrapped = true

UICorner_7.Parent = GetALLItems

Open.Name = "Open"
Open.Parent = ScreenGui
Open.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Open.BorderColor3 = Color3.fromRGB(0, 0, 0)
Open.BorderSizePixel = 0
Open.Position = UDim2.new(0, 0, 0.464102566, 0)
Open.Size = UDim2.new(0, 143, 0, 46)
Open.Font = Enum.Font.JosefinSans
Open.Text = "OPEN"
Open.TextColor3 = Color3.fromRGB(0, 0, 0)
Open.TextScaled = true
Open.TextSize = 14.000
Open.TextWrapped = true

-- Scripts:

local function ZVHAPWJ_fake_script() -- SRANTICHEAT.LocalScript 
	local script = Instance.new('LocalScript', SRANTICHEAT)

	local button = script.Parent
	
	button.MouseButton1Click:Connect(function()
		loadstring(game:HttpGet(("https://raw.githubusercontent.com/BreApple/RobloxUI/main/Slap_Royale/SR_anticheat.txt")))()
	end)
	
end
coroutine.wrap(ZVHAPWJ_fake_script)()
local function GXPW_fake_script() -- Esp.LocalScript 
	local script = Instance.new('LocalScript', Esp)

	local Button = script.Parent
	
	Button.MouseButton1Click:Connect(function()
		local Players = game:GetService("Players"):GetChildren()
		local RunService = game:GetService("RunService")
		local highlight = Instance.new("Highlight")
		highlight.Name = "Highlight"
	
		for i, v in pairs(Players) do
			repeat wait() until v.Character
			if not v.Character:FindFirstChild("HumanoidRootPart"):FindFirstChild("Highlight") then
				local highlightClone = highlight:Clone()
				highlightClone.Adornee = v.Character
				highlightClone.Parent = v.Character:FindFirstChild("HumanoidRootPart")
				highlightClone.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
				highlightClone.Name = "Highlight"
			end
		end
	
		game.Players.PlayerAdded:Connect(function(player)
			repeat wait() until player.Character
			if not player.Character:FindFirstChild("HumanoidRootPart"):FindFirstChild("Highlight") then
				local highlightClone = highlight:Clone()
				highlightClone.Adornee = player.Character
				highlightClone.Parent = player.Character:FindFirstChild("HumanoidRootPart")
				highlightClone.Name = "Highlight"
			end
		end)
	
		game.Players.PlayerRemoving:Connect(function(playerRemoved)
			playerRemoved.Character:FindFirstChild("HumanoidRootPart").Highlight:Destroy()
		end)
	
		RunService.Heartbeat:Connect(function()
			for i, v in pairs(Players) do
				repeat wait() until v.Character
				if not v.Character:FindFirstChild("HumanoidRootPart"):FindFirstChild("Highlight") then
					local highlightClone = highlight:Clone()
					highlightClone.Adornee = v.Character
					highlightClone.Parent = v.Character:FindFirstChild("HumanoidRootPart")
					highlightClone.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
					highlightClone.Name = "Highlight"
					task.wait()
				end
			end
		end)
	end)
	
end
coroutine.wrap(GXPW_fake_script)()
local function EQBUE_fake_script() -- EspItems.LocalScript 
	local script = Instance.new('LocalScript', EspItems)

	local Button = script.Parent
	
	Button.MouseButton1Click:Connect(function()
		for i,v in pairs(game.Workspace:GetDescendants()) do -- grabs everything from workspace
			if v.ClassName == 'TouchTransmitter' and v.Parent.Name == 'Handle' then -- checks if it has a handle and if its a touchtransmitter
				local BillboardGui = Instance.new('BillboardGui') -- Makes Billboardgui
				local TextLabel = Instance.new('TextLabel') -- makes text label
				local Hightlight = Instance.new('Highlight')-- makes hightlight
	
				BillboardGui.Parent = v.Parent -- what the billboardgui goes into
				BillboardGui.AlwaysOnTop = true -- if its on top or not
				BillboardGui.Size = UDim2.new(0, 50, 0, 50) -- size of it
				BillboardGui.StudsOffset = Vector3.new(0,2,0)
	
				TextLabel.Parent = BillboardGui -- putting textlabel into billboardgui
				TextLabel.BackgroundColor3 = Color3.new(1,1,1) -- color
				TextLabel.BackgroundTransparency = 1 -- transparency
				TextLabel.Size = UDim2.new(1, 0, 1, 0) -- size
				TextLabel.Text = v.Parent.Parent.Name -- what the label says
				TextLabel.TextColor3 = Color3.new(1, 0, 0) -- color
				TextLabel.TextScaled = true -- if the text is scaled or not
				
				Hightlight.Parent = v.Parent
				
			end
		end
	end)
	
end
coroutine.wrap(EQBUE_fake_script)()
local function HBQA_fake_script() -- X.LocalScript 
	local script = Instance.new('LocalScript', X)

	local X = script.Parent
	local Frame = script.Parent.Parent.Parent
	local Tittle = script.Parent.Parent
	
	X.MouseButton1Click:Connect(function()
		Frame.Visible = false
		Tittle.Visible = false
		
	end)
	
end
coroutine.wrap(HBQA_fake_script)()
local function TWAD_fake_script() -- GetALLItems.LocalScript 
	local script = Instance.new('LocalScript', GetALLItems)

	local Button = script.Parent
	local Items = {
	
		"Bull's essence"
		
	
	}
	
	Button.MouseButton1Click:Connect(function()
		local Lplr = game.Players.LocalPlayer
		if Lplr.Character.inMatch.Value and workspace:FindFirstChildWhichIsA("Tool") then
			for _, Items in ipairs(game.Workspace:GetDescendants()) do
				if Items:IsA("TouchTransmitter") and Items.Parent.Name == "Handle" then
					Lplr.Character.Humanoid:EquipTool(Items.Parent.Parent)
				end
			end
			Lplr.Character.Humanoid:UnequipTools()
	
		else
	
			print("No Items")
		end
	end)
	
end
coroutine.wrap(TWAD_fake_script)()
local function LHZU_fake_script() -- Open.LocalScript 
	local script = Instance.new('LocalScript', Open)

	local Button = script.Parent
	local Frame = script.Parent.Parent.Frame
	local Tittle = Frame.Tittle
	
	Button.MouseButton1Click:Connect(function()
		Frame.Visible = true
		Tittle.Visible = true
	end)
	
end
coroutine.wrap(LHZU_fake_script)()
